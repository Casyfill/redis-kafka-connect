# https://github.com/redis-field-engineering/redis-github-workflows/blob/main/.github/workflows/build.yml
name: Build

on:
  pull_request:

jobs:
  build:
    name: 'Build'
    uses: redis-field-engineering/redis-github-workflows/.github/workflows/build.yml@main